package abstractionVsInterface;

public interface Shape {

//Interface
	/*
	 * In java, interface is a contract that defines a set of methods that a class
	 * implementing the interface must implement. It provides a way to achieve
	 * multiple inheritance of types.
	 */
	
	double getArea();
	double getPrimeter();
}

